# optional feature `bi` (ADR-0025): Superset, and publishing to it (preview stays available). Add it to any profile:
#   docker compose --env-file .env --env-file deploy/compose/bi.env --profile bi up -d
#   docker compose --env-file .env --env-file deploy/compose/standard.env --env-file deploy/compose/bi.env --profile bi up -d
ANALYSTOS_SUPERSET_URL=http://superset:8088
