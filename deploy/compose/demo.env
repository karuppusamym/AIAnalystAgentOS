# optional feature `demo` (ADR-0025): the ServiceNow mock source, in its own small image. Add it to any profile:
#   docker compose --env-file .env --env-file deploy/compose/demo.env --profile demo up -d
ANALYSTOS_SERVICENOW_MOCK_URL=http://servicenow-mock:8090
