# Ask live check — 2026-09-26 (daily spend cap $1.00)

Live API (`ANALYSTOS_ORCHESTRATOR=local`), OpenRouter key from the shell environment, workspace from
`e2e-20260926-060434` (ServiceNow-shaped mock data; not a connector certification).

| Question | Answered by | Model | Cost (USD) |
|---|---|---|---|
| Show the distribution of incident by category | `rules` (pack `ask_distributions` hint) | — | 0.00000 |
| Which assignment groups have the highest average reassignment count for priority 1 incidents? | `model` (`sql_generation`, rung `llm_large`) | `openai/gpt-5.4-mini` (cheap tier first; no escalation) | 0.00265 |

The first question is the one previously refused with "SQL generation unavailable (no model route)";
the rule rung now answers it without a model. The second needs generated SQL: the cheap tier wrote it
and it passed validation, so the Sonnet escalation tier was not called. `model_call` rows for both
turns record the rung (`rules` skipped / `llm_large` ok) and the cost.

The full scenario (`e2e-20260926-060649`) passed 26/26 with 0 billed model calls: every decision
purpose was answered by the `rules` rung (35,825 tokens recorded as saved).
