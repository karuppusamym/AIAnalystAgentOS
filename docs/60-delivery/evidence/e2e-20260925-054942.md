# MVP end-to-end evidence — 2026-09-25 05:49 UTC

Live run through the HTTP API at `http://localhost:8000` (workspace `ws_de73506cd64e`, run `run_7f201bf634b0`). Raw evidence: `e2e-20260925-054942.json`.
Synthetic ServiceNow-shaped data served by the Table-API mock — **not** a certification of the ServiceNow connector.

**26/26 checks passed.**

| Check | Result |
|---|---|
| 1_workspace_created | PASS |
| 2_servicenow_source_added | PASS |
| 3_incident_and_change_selected | PASS |
| 19_requirement_added_and_replanned | PASS |
| 18_user_can_interrupt_before_publish | PASS |
| approval_requires_approver_role | PASS |
| 4_context_loaded | PASS |
| 5_datasets_profiled | PASS |
| 6_quality_issues_identified | PASS |
| 7_hypotheses_proposed | PASS |
| 8_iterative_analysis | PASS |
| 9_three_evidence_backed_findings | PASS |
| 10_rev_verified | PASS |
| 11_reusable_dataset | PASS |
| 12_kpi_definitions | PASS |
| 13_five_charts | PASS |
| 14_executive_dashboard | PASS |
| 15_operational_dashboard | PASS |
| 16_published_to_superset | PASS |
| 17_queries_artifacts_lineage_stored | PASS |
| 20_evidence_inspectable | PASS |
| safety_restricted_column_rejected | PASS |
| safety_write_rejected | PASS |
| safety_control_plane_unreachable | PASS |
| safety_cross_workspace_isolation | PASS |
| jev_decisioning_used | PASS |

## Verified findings

### I-1 — Priority-1 Record Concentration by CMDB CI Name (confidence 95%)

Among 694 priority-1 records distributed across 39 cmdb ci name segments, the top 3 segments account for 46.7% of the total volume, with a Gini coefficient of 0.57.

Checks: method_fit=ok, sample_size=ok, significance_after_bh=ok, effect_size=ok, no_overreach=ok, reproducible_rerun=ok, second_method=ok

- evidence query `qry_aa8f317a3058` (39 rows, hash `1c29783500d3`)

```sql
SELECT "segment", COUNT(*) AS "n_rows", COUNT(*) AS "volume" FROM (SELECT "cmdb_ci_name" AS "segment" FROM "src_src_0793b800fbce"."incident" WHERE "priority" = 1 AND "category" <> 'inquiry') AS "d" GROUP BY "segment" ORDER BY "volume" DESC NULLS LAST, "segment" LIMIT 5000
```

### I-2 — Missed SLA Rate Varies Across Category (confidence 95%)

The rate of missed SLA is associated with category across 16995 observations, ranging from 12.0% in the hardware segment to 19.3% in the network segment.

Checks: method_fit=ok, sample_size=ok, significance_after_bh=ok, effect_size=ok, no_overreach=ok, reproducible_rerun=ok, second_method=ok

- evidence query `qry_c7c0e628dcb4` (4 rows, hash `15a2d22f0e29`)

```sql
SELECT "segment", COUNT(*) AS "n_rows", COUNT("outcome") AS "n", SUM("outcome") AS "positives" FROM (SELECT "category" AS "segment", CASE WHEN "made_sla" IS NULL THEN NULL WHEN CASE WHEN "made_sla" IS NULL THEN NULL WHEN LOWER(TRIM(CAST("made_sla" AS VARCHAR))) IN ('true', 't', '1', 'yes', 'y') THEN 1 ELSE 0 END = 0 THEN 1 ELSE 0 END AS "outcome" FROM "src_src_0793b800fbce"."incident" WHERE "category" <> 'inquiry') AS "d" GROUP BY "segment" ORDER BY "n_rows" DESC NULLS LAST, "segment" LIMIT 1000
```

### I-3 — Missed sla concentrates in assignment group name = Network Operations (confidence 95%)

Records with assignment group name = Network Operations have a missed SLA rate of 22.1% versus 10.2% for assignment group name = Storage and Backup (2.2x).

Checks: method_fit=ok, sample_size=ok, significance_after_bh=ok, effect_size=ok, no_overreach=ok, reproducible_rerun=ok, second_method=ok

- evidence query `qry_f6f762161df7` (11 rows, hash `c5f9119fbdbf`)

```sql
SELECT "segment", COUNT(*) AS "n_rows", COUNT("outcome") AS "n", SUM("outcome") AS "positives" FROM (SELECT "assignment_group_name" AS "segment", CASE WHEN "made_sla" IS NULL THEN NULL WHEN CASE WHEN "made_sla" IS NULL THEN NULL WHEN LOWER(TRIM(CAST("made_sla" AS VARCHAR))) IN ('true', 't', '1', 'yes', 'y') THEN 1 ELSE 0 END = 0 THEN 1 ELSE 0 END AS "outcome" FROM "src_src_0793b800fbce"."incident" WHERE "category" <> 'inquiry') AS "d" GROUP BY "segment" ORDER BY "n_rows" DESC NULLS LAST, "segment" LIMIT 1000
```

### I-4 — Missed sla concentrates in reassignment count = 3+ (confidence 95%)

Records with reassignment count = 3+ have a missed SLA rate of 28.8% versus 9.7% for reassignment count = 1 (3.0x).

Checks: method_fit=ok, sample_size=ok, significance_after_bh=ok, effect_size=ok, no_overreach=ok, reproducible_rerun=ok, second_method=ok

- evidence query `qry_ccfae4b5012c` (4 rows, hash `0c673461691c`)

```sql
SELECT "segment", "segment_order", COUNT(*) AS "n_rows", COUNT("outcome") AS "n", SUM("outcome") AS "positives" FROM (SELECT CASE WHEN "reassignment_count" IS NULL THEN NULL WHEN "reassignment_count" < 0.0 THEN '(<0)' WHEN "reassignment_count" < 1.0 THEN '0' WHEN "reassignment_count" < 2.0 THEN '1' WHEN "reassignment_count" < 3.0 THEN '2' ELSE '3+' END AS "segment", CASE WHEN "reassignment_count" IS NULL THEN NULL WHEN "reassignment_count" < 0.0 THEN -1 WHEN "reassignment_count" < 1.0 THEN 0 WHEN "reassignment_count" < 2.0 THEN 1 WHEN "reassignment_count" < 3.0 THEN 2 ELSE 3 END AS "segment_or
```

### I-5 — Assignment Group Concentration in Payments Gateway Priority-1 Incidents (confidence 95%)

Among 271 priority-1 incidents tied to the Payments Gateway configuration item, the top 3 assignment groups account for 83.4% of the volume, with a Gini coefficient of 0.66.

Checks: method_fit=ok, sample_size=ok, significance_after_bh=ok, effect_size=ok, no_overreach=ok, reproducible_rerun=ok, second_method=ok

- evidence query `qry_f93c1c870e5f` (11 rows, hash `790237a9d8e2`)

```sql
SELECT "segment", COUNT(*) AS "n_rows", COUNT(*) AS "volume" FROM (SELECT "assignment_group_name" AS "segment" FROM "src_src_0793b800fbce"."incident" WHERE "category" <> 'inquiry' AND "priority" = 1 AND "cmdb_ci_name" = 'Payments Gateway') AS "d" GROUP BY "segment" ORDER BY "volume" DESC NULLS LAST, "segment" LIMIT 5000
```

## KPIs

| Metric | Expression | Value |
|---|---|---|
| repeat_critical_incident_rate | `COUNT(CASE WHEN priority = 1 THEN 1 END) * 1.0 / COUNT(*)` | 0.04083553986466608 |
| record_count | `COUNT(*)` | 16995 |
| avg_reassignment_count | `AVG("reassignment_count")` | 1.337805236834363 |
| avg_reopen_count | `AVG("reopen_count")` | 0.052015298617240364 |
| sla_breach_rate | `AVG(CASE WHEN made_sla = false THEN 1.0 ELSE 0 END) * 100` | 14.657252132980288 |
| high_reassignment_incident_rate | `AVG(CASE WHEN reassignment_count >= 3 THEN 1.0 ELSE 0 END) * 100` | 16.581347455133862 |
| reopen_rate | `AVG(CASE WHEN reopen_count > 0 THEN 1.0 ELSE 0 END) * 100` | 5.07796410709032 |
| critical_incident_count | `COUNT(CASE WHEN priority = 1 THEN 1 END)` | 694 |
| avg_reassignment_by_group | `AVG(reassignment_count)` | 1.337805236834363 |

## Publication

- executive: http://localhost:8088/superset/dashboard/21/
- operational: http://localhost:8088/superset/dashboard/22/

## Model usage

```json
{
  "model_calls": 41,
  "jev_calls": 19,
  "failed_calls": 1,
  "cost_usd": 0.31860354999999985,
  "purposes": [
    "chart_selection",
    "feedback_classification",
    "feedback_interpretation",
    "follow_up_generation",
    "hypothesis_generation",
    "hypothesis_priority",
    "insight_narrative",
    "planning",
    "rev_second_opinion",
    "risk_check",
    "semantic_modeling",
    "stop_check",
    "summarization",
    "verification"
  ]
}
```

## Redirect (dynamic replanning)

```json
{
  "kind": "redirect",
  "classified_by": "jev:typesafe/jev-1.13-20260917",
  "consequential_p": 0.09,
  "interpretation": {
    "filters": [
      {
        "asset": "src_src_0793b800fbce.incident",
        "column": "category",
        "op": "!=",
        "value": "inquiry"
      }
    ],
    "focus": [
      "Exclude inquiry-category incidents from the analysis; they are requests, not operational failures."
    ],
    "summary": "Exclude incidents categorized as inquiry from the analysis since they represent requests rather than operational failures.",
    "interpreted_by": "anthropic/claude-sonnet-5"
  },
  "replan": {
    "plan_version": 2,
    "tasks_reset": 9,
    "tasks_removed": 9,
    "approvals_invalidated": 0
  }
}
```

