# MVP end-to-end evidence — 2026-09-25 17:11 UTC

Live run through the HTTP API at `http://localhost:8000` (workspace `ws_f7ca7e9f99db`, run `run_9bfa114f4a58`). Raw evidence: `e2e-20260925-171115.json`.
Synthetic ServiceNow-shaped data served by the Table-API mock — **not** a certification of the ServiceNow connector.

**26/26 checks passed.**

Duration: scenario 382.2 s (started 17:11:15, finished 17:17:37 UTC); analysis run 376.0 s (started → finished, including the approval wait and the redirect).

| Check | Result |
|---|---|
| 1_workspace_created | PASS |
| 2_servicenow_source_added | PASS |
| 3_incident_and_change_selected | PASS |
| 19_requirement_added_and_replanned | PASS |
| 18_user_can_interrupt_before_publish | PASS |
| approval_requires_approver_role | PASS |
| 4_context_loaded | PASS |
| 5_datasets_profiled | PASS |
| 6_quality_issues_identified | PASS |
| 7_hypotheses_proposed | PASS |
| 8_iterative_analysis | PASS |
| 9_three_evidence_backed_findings | PASS |
| 10_rev_verified | PASS |
| 11_reusable_dataset | PASS |
| 12_kpi_definitions | PASS |
| 13_five_charts | PASS |
| 14_executive_dashboard | PASS |
| 15_operational_dashboard | PASS |
| 16_published_to_superset | PASS |
| 17_queries_artifacts_lineage_stored | PASS |
| 20_evidence_inspectable | PASS |
| safety_restricted_column_rejected | PASS |
| safety_write_rejected | PASS |
| safety_control_plane_unreachable | PASS |
| safety_cross_workspace_isolation | PASS |
| jev_decisioning_used | PASS |

## Verified findings

### I-1 — Payments Gateway Incidents Concentrated in Software Category (confidence 90%)

92.1% of Priority-1 incidents in Payments Gateway are from the software category, with the top three categories accounting for 97.8% of all 278 incidents.

Checks: method_fit=ok, sample_size=ok, significance_after_bh=ok, effect_size=ok, representative_population=ok, no_overreach=ok, reproducible_rerun=ok, second_method=ok

- evidence query `qry_e53ffa4d5282` (4 rows, hash `d7187f14723a`)

```sql
SELECT "segment", COUNT(*) AS "n_rows", COUNT(*) AS "volume" FROM (SELECT "category" AS "segment" FROM "src_src_f3076541687e"."incident" WHERE "priority" = 1 AND "cmdb_ci_name" = 'Payments Gateway' AND "category" <> 'inquiry') AS "d" GROUP BY "segment" ORDER BY "volume" DESC NULLS LAST, "segment" LIMIT 5000
```

### I-2 — Concentration of Priority-1 Records Across CMDB CI Names (confidence 95%)

Analysis of 694 priority-1 records shows that a small number of cmdb ci name values account for a disproportionate share of volume, with a Gini coefficient of 0.57 and the top 3 segments holding 46.7% of the total volume.

Checks: method_fit=ok, sample_size=ok, significance_after_bh=ok, effect_size=ok, representative_population=ok, no_overreach=ok, reproducible_rerun=ok, second_method=ok

- evidence query `qry_6253ec8656df` (39 rows, hash `1c29783500d3`)

```sql
SELECT "segment", COUNT(*) AS "n_rows", COUNT(*) AS "volume" FROM (SELECT "cmdb_ci_name" AS "segment" FROM "src_src_f3076541687e"."incident" WHERE "priority" = 1 AND "category" <> 'inquiry') AS "d" GROUP BY "segment" ORDER BY "volume" DESC NULLS LAST, "segment" LIMIT 5000
```

### I-3 — SLA Miss Rates Vary by Assignment Group (confidence 95%)

The rate of missed SLA ranges from a baseline of 10.2% in Storage and Backup to a top rate of 22.1% in Network Operations, with an overall rate of 14.7% across 16499 observations.

Checks: method_fit=ok, sample_size=ok, significance_after_bh=ok, effect_size=ok, representative_population=ok, no_overreach=ok, reproducible_rerun=ok, second_method=ok

- evidence query `qry_9492e280f191` (11 rows, hash `c5f9119fbdbf`)

```sql
SELECT "segment", COUNT(*) AS "n_rows", COUNT("outcome") AS "n", SUM("outcome") AS "positives" FROM (SELECT "assignment_group_name" AS "segment", CASE WHEN "made_sla" IS NULL THEN NULL WHEN CASE WHEN "made_sla" IS NULL THEN NULL WHEN LOWER(TRIM(CAST("made_sla" AS VARCHAR))) IN ('true', 't', '1', 'yes', 'y') THEN 1 ELSE 0 END = 0 THEN 1 ELSE 0 END AS "outcome" FROM "src_src_f3076541687e"."incident" WHERE "category" <> 'inquiry') AS "d" GROUP BY "segment" ORDER BY "n_rows" DESC NULLS LAST, "segment" LIMIT 1000
```

### I-4 — SLA Miss Rates Vary by Category (confidence 95%)

The rate of missed SLA varies across categories (n=16995), with the network category showing a rate of 19.3% compared to the overall rate of 14.7%.

Checks: method_fit=ok, sample_size=ok, significance_after_bh=ok, effect_size=ok, representative_population=ok, no_overreach=ok, reproducible_rerun=ok, second_method=ok

- evidence query `qry_271196afc34c` (4 rows, hash `15a2d22f0e29`)

```sql
SELECT "segment", COUNT(*) AS "n_rows", COUNT("outcome") AS "n", SUM("outcome") AS "positives" FROM (SELECT "category" AS "segment", CASE WHEN "made_sla" IS NULL THEN NULL WHEN CASE WHEN "made_sla" IS NULL THEN NULL WHEN LOWER(TRIM(CAST("made_sla" AS VARCHAR))) IN ('true', 't', '1', 'yes', 'y') THEN 1 ELSE 0 END = 0 THEN 1 ELSE 0 END AS "outcome" FROM "src_src_f3076541687e"."incident" WHERE "category" <> 'inquiry') AS "d" GROUP BY "segment" ORDER BY "n_rows" DESC NULLS LAST, "segment" LIMIT 1000
```

### I-5 — SLA Miss Rates Vary by Reassignment Count (confidence 95%)

Across 16995 observations, the missed SLA rate is associated with reassignment count, with the 3+ segment reaching a rate of 28.8% compared to the overall rate of 14.7%.

Checks: method_fit=ok, sample_size=ok, significance_after_bh=ok, effect_size=ok, representative_population=ok, no_overreach=ok, reproducible_rerun=ok, second_method=ok

- evidence query `qry_9be06647c3c5` (4 rows, hash `0c673461691c`)

```sql
SELECT "segment", "segment_order", COUNT(*) AS "n_rows", COUNT("outcome") AS "n", SUM("outcome") AS "positives" FROM (SELECT CASE WHEN "reassignment_count" IS NULL THEN NULL WHEN "reassignment_count" < 0.0 THEN '(<0)' WHEN "reassignment_count" < 1.0 THEN '0' WHEN "reassignment_count" < 2.0 THEN '1' WHEN "reassignment_count" < 3.0 THEN '2' ELSE '3+' END AS "segment", CASE WHEN "reassignment_count" IS NULL THEN NULL WHEN "reassignment_count" < 0.0 THEN -1 WHEN "reassignment_count" < 1.0 THEN 0 WHEN "reassignment_count" < 2.0 THEN 1 WHEN "reassignment_count" < 3.0 THEN 2 ELSE 3 END AS "segment_or
```

### I-6 — Missed-SLA Rates Vary Across Assignment Groups for Network Incidents (confidence 95%)

Among 4020 network category incidents, missed-SLA rates vary significantly across assignment groups, ranging from 12.8% for Cloud Platform to 22.1% for Network Operations.

Checks: method_fit=ok, sample_size=ok, significance_after_bh=ok, effect_size=ok, representative_population=ok, no_overreach=ok, reproducible_rerun=ok, second_method=ok

- evidence query `qry_ce9f28bad14d` (4 rows, hash `9940086d68b5`)

```sql
SELECT "segment", COUNT(*) AS "n_rows", COUNT("outcome") AS "n", SUM("outcome") AS "positives" FROM (SELECT "assignment_group_name" AS "segment", CASE WHEN "made_sla" IS NULL THEN NULL WHEN CASE WHEN "made_sla" IS NULL THEN NULL WHEN LOWER(TRIM(CAST("made_sla" AS VARCHAR))) IN ('true', 't', '1', 'yes', 'y') THEN 1 ELSE 0 END = 0 THEN 1 ELSE 0 END AS "outcome" FROM "src_src_f3076541687e"."incident" WHERE "category" = 'network' AND "category" <> 'inquiry') AS "d" GROUP BY "segment" ORDER BY "n_rows" DESC NULLS LAST, "segment" LIMIT 1000
```

### I-7 — Reassignment Count Varies Across Assignment Groups (confidence 90%)

Reassignment counts differ significantly across the 16499 assignment groups, with Network Operations showing a mean of 2.69 compared to a baseline mean of 0.73 for Storage and Backup.

Checks: method_fit=ok, sample_size=ok, significance_after_bh=ok, effect_size=ok, representative_population=ok, no_overreach=ok, reproducible_rerun=ok, second_method=ok

- evidence query `qry_12f1fa456035` (11 rows, hash `9ce7a88b9121`)

```sql
SELECT "segment", COUNT(*) AS "n_rows", COUNT("outcome") AS "n", AVG("outcome") AS "mean", MIN("outcome") AS "min", MAX("outcome") AS "max", PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY "outcome") AS "median" FROM (SELECT "assignment_group_name" AS "segment", CAST("reassignment_count" AS DOUBLE PRECISION) AS "outcome" FROM "src_src_f3076541687e"."incident" WHERE "category" <> 'inquiry') AS "d" GROUP BY "segment" ORDER BY "n_rows" DESC NULLS LAST, "segment" LIMIT 1000
```

- evidence query `qry_d9cfd4d0ef9f` (16499 rows, hash `27ecbf880ee8`)

```sql
SELECT "s"."segment" AS "segment", "s"."outcome" AS "outcome", "s"."_rows_total" AS "_rows_total", "s"."_rows_excluded" AS "_rows_excluded" FROM (SELECT "d"."segment" AS "segment", "d"."outcome" AS "outcome", ROW_NUMBER() OVER (PARTITION BY "d"."segment", "d"."outcome" ORDER BY "d"."segment") AS "_rn", COUNT(*) OVER () AS "_rows_total", SUM(CASE WHEN "d"."segment" IS NULL OR "d"."outcome" IS NULL THEN 1 ELSE 0 END) OVER () AS "_rows_excluded" FROM (SELECT "assignment_group_name" AS "segment", CAST("reassignment_count" AS DOUBLE PRECISION) AS "outcome" FROM "src_src_f3076541687e"."incident" WHE
```

### I-9 — Category is network concentrates in assignment group name = Network Operations (confidence 80%)

Records with assignment group name = Network Operations have a category is network rate of 100.0% versus 0.0% for assignment group name = Application Support.

Checks: method_fit=ok, sample_size=ok, significance_after_bh=ok, effect_size=ok, representative_population=ok, no_overreach=ok, reproducible_rerun=ok, second_method=ok

- evidence query `qry_097c9bc71192` (11 rows, hash `7f45c96cf098`)

```sql
SELECT "segment", COUNT(*) AS "n_rows", COUNT("outcome") AS "n", SUM("outcome") AS "positives" FROM (SELECT "assignment_group_name" AS "segment", CASE WHEN "category" IS NULL THEN NULL WHEN "category" = 'network' THEN 1 ELSE 0 END AS "outcome" FROM "src_src_f3076541687e"."incident" WHERE "category" <> 'inquiry') AS "d" GROUP BY "segment" ORDER BY "n_rows" DESC NULLS LAST, "segment" LIMIT 1000
```

## KPIs

| Metric | Expression | Value |
|---|---|---|
| critical_incident_rate | `AVG(CASE WHEN priority=1 THEN 1.0 ELSE 0.0 END)` | 0.04083553986466608 |
| critical_incident_count | `SUM(CASE WHEN priority=1 THEN 1 ELSE 0 END)` | 694 |
| record_count | `COUNT(*)` | 16995 |
| avg_reassignment_count | `AVG("reassignment_count")` | 1.337805236834363 |
| avg_reopen_count | `AVG("reopen_count")` | 0.052015298617240364 |
| reopen_rate | `AVG(CASE WHEN reopen_count>0 THEN 1.0 ELSE 0.0 END)` | 0.05077964107090321 |
| high_reassignment_rate | `AVG(CASE WHEN reassignment_count>=3 THEN 1.0 ELSE 0.0 END)` | 0.16581347455133863 |

## Publication

- executive: http://localhost:8088/superset/dashboard/57/
- operational: http://localhost:8088/superset/dashboard/58/

## Model usage

```json
{
  "model_calls": 53,
  "jev_calls": 22,
  "failed_calls": 3,
  "cost_usd": 0.29407893583999994,
  "purposes": [
    "chart_selection",
    "feedback_classification",
    "feedback_interpretation",
    "follow_up_generation",
    "hypothesis_generation",
    "hypothesis_priority",
    "insight_narrative",
    "planning",
    "rev_second_opinion",
    "risk_check",
    "semantic_modeling",
    "stop_check",
    "summarization",
    "verification"
  ]
}
```

## Redirect (dynamic replanning)

```json
{
  "kind": "redirect",
  "classified_by": "jev:typesafe/jev-1.13-20260917",
  "consequential_p": 0.1,
  "interpretation": {
    "filters": [
      {
        "asset": "src_src_f3076541687e.incident",
        "column": "category",
        "op": "!=",
        "value": "inquiry"
      }
    ],
    "focus": [
      "SLA breach drivers",
      "recurring operational problems",
      "teams with high reassignment",
      "applications with repeated critical incidents"
    ],
    "summary": "Exclude incidents categorized as inquiries from the analysis since they represent requests rather than operational failures.",
    "interpreted_by": "anthropic/claude-sonnet-5"
  },
  "replan": {
    "plan_version": 2,
    "tasks_reset": 9,
    "tasks_removed": 9,
    "approvals_invalidated": 0
  }
}
```

