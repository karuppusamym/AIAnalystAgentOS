# MVP end-to-end evidence — 2026-09-25 18:00 UTC

Live run through the HTTP API at `http://localhost:8000` (workspace `ws_22350b7da336`, run `run_3df362700fc9`). Raw evidence: `e2e-20260925-180003.json`.
Synthetic ServiceNow-shaped data served by the Table-API mock — **not** a certification of the ServiceNow connector.

**26/26 checks passed.**

Duration: scenario 38.1 s (started 18:00:03, finished 18:00:41 UTC); analysis run 30.8 s (started → finished, including the approval wait and the redirect).

| Check | Result |
|---|---|
| 1_workspace_created | PASS |
| 2_servicenow_source_added | PASS |
| 3_incident_and_change_selected | PASS |
| 19_requirement_added_and_replanned | PASS |
| 18_user_can_interrupt_before_publish | PASS |
| approval_requires_approver_role | PASS |
| 4_context_loaded | PASS |
| 5_datasets_profiled | PASS |
| 6_quality_issues_identified | PASS |
| 7_hypotheses_proposed | PASS |
| 8_iterative_analysis | PASS |
| 9_three_evidence_backed_findings | PASS |
| 10_rev_verified | PASS |
| 11_reusable_dataset | PASS |
| 12_kpi_definitions | PASS |
| 13_five_charts | PASS |
| 14_executive_dashboard | PASS |
| 15_operational_dashboard | PASS |
| 16_published_to_superset | PASS |
| 17_queries_artifacts_lineage_stored | PASS |
| 20_evidence_inspectable | PASS |
| safety_restricted_column_rejected | PASS |
| safety_write_rejected | PASS |
| safety_control_plane_unreachable | PASS |
| safety_cross_workspace_isolation | PASS |
| jev_decisioning_used | PASS |

## Verified findings

### I-1 — Volume is concentrated in few cmdb ci name values (confidence 85%)

Payments Gateway accounts for 40.1% of records (where priority = 1).

Checks: method_fit=ok, sample_size=ok, significance_after_bh=ok, effect_size=ok, representative_population=ok, no_overreach=ok, reproducible_rerun=ok, second_method=ok

- evidence query `qry_fa8e5bb3d0e4` (39 rows, hash `1c29783500d3`)

```sql
SELECT "segment", COUNT(*) AS "n_rows", COUNT(*) AS "volume" FROM (SELECT "cmdb_ci_name" AS "segment" FROM "src_src_a6d42fef4667"."incident" WHERE "priority" = 1 AND "category" <> 'inquiry') AS "d" GROUP BY "segment" ORDER BY "volume" DESC NULLS LAST, "segment" LIMIT 5000
```

### I-2 — Missed SLA concentrates in assignment group name = Network Operations (confidence 90%)

Records with assignment group name = Network Operations have a missed SLA rate of 22.1% versus 10.2% for assignment group name = Storage and Backup (2.2x).

Checks: method_fit=ok, sample_size=ok, significance_after_bh=ok, effect_size=ok, representative_population=ok, no_overreach=ok, reproducible_rerun=ok, second_method=ok

- evidence query `qry_37689c94353a` (11 rows, hash `c5f9119fbdbf`)

```sql
SELECT "segment", COUNT(*) AS "n_rows", COUNT("outcome") AS "n", SUM("outcome") AS "positives" FROM (SELECT "assignment_group_name" AS "segment", CASE WHEN "made_sla" IS NULL THEN NULL WHEN CASE WHEN "made_sla" IS NULL THEN NULL WHEN LOWER(TRIM(CAST("made_sla" AS VARCHAR))) IN ('true', 't', '1', 'yes', 'y') THEN 1 ELSE 0 END = 0 THEN 1 ELSE 0 END AS "outcome" FROM "src_src_a6d42fef4667"."incident" WHERE "category" <> 'inquiry') AS "d" GROUP BY "segment" ORDER BY "n_rows" DESC NULLS LAST, "segment" LIMIT 1000
```

### I-3 — Missed SLA concentrates in category = network (confidence 90%)

Records with category = network have a missed SLA rate of 19.3% versus 12.0% for category = hardware (1.6x).

Checks: method_fit=ok, sample_size=ok, significance_after_bh=ok, effect_size=ok, representative_population=ok, no_overreach=ok, reproducible_rerun=ok, second_method=ok

- evidence query `qry_7e38760da205` (4 rows, hash `15a2d22f0e29`)

```sql
SELECT "segment", COUNT(*) AS "n_rows", COUNT("outcome") AS "n", SUM("outcome") AS "positives" FROM (SELECT "category" AS "segment", CASE WHEN "made_sla" IS NULL THEN NULL WHEN CASE WHEN "made_sla" IS NULL THEN NULL WHEN LOWER(TRIM(CAST("made_sla" AS VARCHAR))) IN ('true', 't', '1', 'yes', 'y') THEN 1 ELSE 0 END = 0 THEN 1 ELSE 0 END AS "outcome" FROM "src_src_a6d42fef4667"."incident" WHERE "category" <> 'inquiry') AS "d" GROUP BY "segment" ORDER BY "n_rows" DESC NULLS LAST, "segment" LIMIT 1000
```

### I-4 — Missed SLA concentrates in reassignment count = 3+ (confidence 90%)

Records with reassignment count = 3+ have a missed SLA rate of 29.1% versus 9.6% for reassignment count = 1 (3.0x).

Checks: method_fit=ok, sample_size=ok, significance_after_bh=ok, effect_size=ok, representative_population=ok, no_overreach=ok, reproducible_rerun=ok, second_method=ok

- evidence query `qry_920ab2d22488` (4 rows, hash `825eb2f6594c`)

```sql
SELECT "segment", "segment_order", COUNT(*) AS "n_rows", COUNT("outcome") AS "n", SUM("outcome") AS "positives" FROM (SELECT CASE WHEN "reassignment_count" IS NULL THEN NULL WHEN "reassignment_count" < 0.0 THEN '(<0)' WHEN "reassignment_count" < 1.0 THEN '0' WHEN "reassignment_count" < 2.0 THEN '1' WHEN "reassignment_count" < 3.0 THEN '2' ELSE '3+' END AS "segment", CASE WHEN "reassignment_count" IS NULL THEN NULL WHEN "reassignment_count" < 0.0 THEN -1 WHEN "reassignment_count" < 1.0 THEN 0 WHEN "reassignment_count" < 2.0 THEN 1 WHEN "reassignment_count" < 3.0 THEN 2 ELSE 3 END AS "segment_or
```

### I-5 — Resolution hours is higher for cmdb CI name = API Gateway (confidence 85%)

Median resolution hours is 42.1 for cmdb CI name = API Gateway versus 15.2 for Payments Gateway.

Checks: method_fit=ok, sample_size=ok, significance_after_bh=ok, effect_size=ok, representative_population=ok, no_overreach=ok, reproducible_rerun=ok, second_method=ok

- evidence query `qry_a0f1d3a3d7a0` (40 rows, hash `3d634c320d7b`)

```sql
SELECT "segment", COUNT(*) AS "n_rows", COUNT("outcome") AS "n", AVG("outcome") AS "mean", MIN("outcome") AS "min", MAX("outcome") AS "max", PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY "outcome") AS "median" FROM (SELECT "cmdb_ci_name" AS "segment", CAST(CAST((EXTRACT(EPOCH FROM CAST("resolved_at" AS TIMESTAMP)) - EXTRACT(EPOCH FROM CAST("opened_at" AS TIMESTAMP))) AS DOUBLE PRECISION) / 3600.0 AS DOUBLE PRECISION) AS "outcome" FROM "src_src_a6d42fef4667"."incident" WHERE "category" <> 'inquiry') AS "d" GROUP BY "segment" ORDER BY "n_rows" DESC NULLS LAST, "segment" LIMIT 1000
```

- evidence query `qry_1e6e316cd05a` (16905 rows, hash `5ecb0f5feaa7`)

```sql
SELECT "s"."segment" AS "segment", "s"."outcome" AS "outcome", "s"."_rows_total" AS "_rows_total", "s"."_rows_excluded" AS "_rows_excluded" FROM (SELECT "d"."segment" AS "segment", "d"."outcome" AS "outcome", ROW_NUMBER() OVER (PARTITION BY "d"."segment", "d"."outcome" ORDER BY "d"."segment") AS "_rn", COUNT(*) OVER () AS "_rows_total", SUM(CASE WHEN "d"."segment" IS NULL OR "d"."outcome" IS NULL THEN 1 ELSE 0 END) OVER () AS "_rows_excluded" FROM (SELECT "cmdb_ci_name" AS "segment", CAST(CAST((EXTRACT(EPOCH FROM CAST("resolved_at" AS TIMESTAMP)) - EXTRACT(EPOCH FROM CAST("opened_at" AS TIMEST
```

### I-6 — Missed SLA concentrates in assignment group name = Network Operations (confidence 90%)

Records with assignment group name = Network Operations have a missed SLA rate of 22.1% versus 12.8% for assignment group name = Cloud Platform (1.7x) (where category = network).

Checks: method_fit=ok, sample_size=ok, significance_after_bh=ok, effect_size=ok, representative_population=ok, no_overreach=ok, reproducible_rerun=ok, second_method=ok

- evidence query `qry_675a874a50bf` (4 rows, hash `9940086d68b5`)

```sql
SELECT "segment", COUNT(*) AS "n_rows", COUNT("outcome") AS "n", SUM("outcome") AS "positives" FROM (SELECT "assignment_group_name" AS "segment", CASE WHEN "made_sla" IS NULL THEN NULL WHEN CASE WHEN "made_sla" IS NULL THEN NULL WHEN LOWER(TRIM(CAST("made_sla" AS VARCHAR))) IN ('true', 't', '1', 'yes', 'y') THEN 1 ELSE 0 END = 0 THEN 1 ELSE 0 END AS "outcome" FROM "src_src_a6d42fef4667"."incident" WHERE "category" = 'network' AND "category" <> 'inquiry') AS "d" GROUP BY "segment" ORDER BY "n_rows" DESC NULLS LAST, "segment" LIMIT 1000
```

## KPIs

| Metric | Expression | Value |
|---|---|---|
| median_opened_at_to_resolved_at_hours | `PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY "opened_at_to_resolved_at_hours")` | 38.61972222222222 |
| record_count | `COUNT(*)` | 16995 |
| avg_reassignment_count | `AVG("reassignment_count")` | 1.337805236834363 |
| avg_reopen_count | `AVG("reopen_count")` | 0.052015298617240364 |
| avg_opened_at_to_resolved_at_hours | `AVG("opened_at_to_resolved_at_hours")` | 53.444464376088796 |

## Publication

- executive: http://localhost:8088/superset/dashboard/81/
- operational: http://localhost:8088/superset/dashboard/82/

## Model usage

```json
{
  "model_calls": 19,
  "jev_calls": 17,
  "failed_calls": 2,
  "cost_usd": 0.00040992,
  "purposes": [
    "chart_selection",
    "feedback_classification",
    "feedback_interpretation",
    "hypothesis_priority",
    "planning",
    "rev_second_opinion",
    "risk_check",
    "stop_check"
  ]
}
```

## Redirect (dynamic replanning)

```json
{
  "kind": "redirect",
  "classified_by": "jev:typesafe/jev-1.13-20260917",
  "consequential_p": 0.11,
  "interpretation": {
    "filters": [
      {
        "asset": "src_src_a6d42fef4667.incident",
        "column": "category",
        "op": "!=",
        "value": "inquiry"
      }
    ],
    "focus": [
      "Exclude inquiry-category incidents from the analysis; they are requests, not operational failures."
    ],
    "summary": "Exclude inquiry-category incidents from the analysis; they are requests, not operational failures.",
    "interpreted_by": "rules"
  },
  "replan": {
    "plan_version": 2,
    "tasks_reset": 9,
    "tasks_removed": 9,
    "approvals_invalidated": 0
  }
}
```

