# SSE load evidence — 2026-09-25 16:50 UTC

`python scripts/load_sse.py --streams 200 --runs 50 --seconds 60 --rate 1 --reconnect 20` against a private uvicorn on a test database (`localhost:5432/analystos_test_c`), not the running API. Redis nudges: on. Raw: `sse-load-20260925-165049.json`. Server measurements cover the load window (first client connect to the last `end` frame), not the server's warm-up; the measured server runs the stdlib asyncio loop so slow callbacks can be timed (production uvicorn uses uvloop, which is faster).

**6/6 checks passed.**

| Check | Result |
|---|---|
| p95_latency_below_1s | PASS |
| no_event_loop_stall_warnings | PASS |
| every_delivery_received_once_in_order | PASS |
| resume_with_last_event_id | PASS |
| every_stream_ended | PASS |
| no_errors | PASS |

| Measure | Value |
|---|---|
| Concurrent streams / runs | 200 / 50 |
| Events emitted / deliveries expected / received | 3000 / 12000 / 12000 |
| Event latency p50 / p95 / p99 / max (commit→client, ms) | 14.3 / 38.9 / 95.7 / 148.2 |
| Server loop lag p50 / p95 / p99 / max (ms, 1276 samples @ 50 ms) | 0.55 / 2.27 / 8.22 / 119.59 |
| Slow-callback (stall) warnings: one callback holding the loop >= 100 ms | 0 |
| Probe lag samples > 100 ms (s after connect: ms) | 1 (0.4 s: 119.6 ms) |
| Full (gen-2) GC pauses > 20 ms (s: ms) | (0.4 s: 35.9 ms), (12.3 s: 66.6 ms), (29.3 s: 84.6 ms), (45.3 s: 84.3 ms), (62.2 s: 64.5 ms) |
| Streams complete and in order | 200/200 |
| Streams resumed with Last-Event-ID, complete | 20/20 |
| Streams ended with `end` frame | 200/200 |
| Stream connect (auth + response headers) p95, ms | 1454.4 |
