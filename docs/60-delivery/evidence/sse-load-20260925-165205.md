# SSE load evidence — 2026-09-25 16:52 UTC

`python scripts/load_sse.py --streams 200 --runs 50 --seconds 60 --rate 1 --reconnect 20 --no-redis` against a private uvicorn on a test database (`localhost:5432/analystos_test_c`), not the running API. Redis nudges: off (polling fallback). Raw: `sse-load-20260925-165205.json`. Server measurements cover the load window (first client connect to the last `end` frame), not the server's warm-up; the measured server runs the stdlib asyncio loop so slow callbacks can be timed (production uvicorn uses uvloop, which is faster).

**6/6 checks passed.**

| Check | Result |
|---|---|
| p95_latency_below_1s | PASS |
| no_event_loop_stall_warnings | PASS |
| every_delivery_received_once_in_order | PASS |
| resume_with_last_event_id | PASS |
| every_stream_ended | PASS |
| no_errors | PASS |

| Measure | Value |
|---|---|
| Concurrent streams / runs | 200 / 50 |
| Events emitted / deliveries expected / received | 3000 / 12000 / 12000 |
| Event latency p50 / p95 / p99 / max (commit→client, ms) | 270.3 / 501.3 / 524.0 / 630.6 |
| Server loop lag p50 / p95 / p99 / max (ms, 1274 samples @ 50 ms) | 0.83 / 4.16 / 11.68 / 123.44 |
| Slow-callback (stall) warnings: one callback holding the loop >= 100 ms | 0 |
| Probe lag samples > 100 ms (s after connect: ms) | 1 (0.4 s: 123.4 ms) |
| Full (gen-2) GC pauses > 20 ms (s: ms) | (0.4 s: 36.8 ms), (7.2 s: 85.5 ms), (14.7 s: 65.2 ms), (22.3 s: 80.2 ms), (29.6 s: 110.2 ms), (36.9 s: 64.7 ms), (44.1 s: 82.9 ms), (51.4 s: 102.7 ms), (58.5 s: 59.3 ms) |
| Streams complete and in order | 200/200 |
| Streams resumed with Last-Event-ID, complete | 20/20 |
| Streams ended with `end` frame | 200/200 |
| Stream connect (auth + response headers) p95, ms | 1617.7 |
