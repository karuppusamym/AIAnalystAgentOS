# SSE load evidence — 2026-09-25 23:28 UTC

`python scripts/load_sse.py --streams 500 --runs 50 --seconds 30 --rate 1 --reconnect 50` against a private uvicorn on a test database (`localhost:5432/analystos_load_s05s`), not the running API. Redis nudges: on. Raw: `sse-load-20260925-232837.json`. Server measurements cover the load window (first client connect to the last `end` frame), not the server's warm-up; the measured server runs the stdlib asyncio loop so slow callbacks can be timed (production uvicorn uses uvloop, which is faster).

**6/6 checks passed.**

| Check | Result |
|---|---|
| p95_latency_below_1s | PASS |
| no_event_loop_stall_warnings | PASS |
| every_delivery_received_once_in_order | PASS |
| resume_with_last_event_id | PASS |
| every_stream_ended | PASS |
| no_errors | PASS |

| Measure | Value |
|---|---|
| Concurrent streams / runs | 500 / 50 |
| Events emitted / deliveries expected / received | 1500 / 15000 / 15000 |
| Event latency p50 / p95 / p99 / max (commit→client, ms) | 19.7 / 154.4 / 1084.6 / 1369.7 |
| Server loop lag p50 / p95 / p99 / max (ms, 687 samples @ 50 ms) | 0.47 / 5.7 / 22.44 / 175.56 |
| Slow-callback (stall) warnings: one callback holding the loop >= 100 ms | 0 |
| Probe lag samples > 100 ms (s after connect: ms) | 1 (0.8 s: 175.6 ms) |
| Full (gen-2) GC pauses > 20 ms (s: ms) | (0.6 s: 22.1 ms), (2.9 s: 42.3 ms), (15.1 s: 50.7 ms), (29.1 s: 73.3 ms) |
| Streams complete and in order | 500/500 |
| Streams resumed with Last-Event-ID, complete | 50/50 |
| Streams ended with `end` frame | 500/500 |
| Stream connect (auth + response headers) p95, ms | 3714.4 |
