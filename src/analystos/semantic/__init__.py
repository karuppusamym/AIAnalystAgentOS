"""Workspace semantic layer (P4-K03): Apache Ossie 0.1.1 model, metric approval workflow, dbt/Superset interchange.

`ossie` and `dbt` are pure, version-pinned adapters (ADR-0013 §5); `service` is the persisted,
governed side (versions, proposals, approvals, conflicts, the publish gate).
"""
